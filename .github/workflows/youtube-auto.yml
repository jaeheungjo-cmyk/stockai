name: YouTube Auto Collect (비활성화됨)

on:
  workflow_dispatch:  # 자동 스케줄 제거 — 수동 실행만 가능 (현재 사용 안 함)

jobs:
  collect:
    runs-on: ubuntu-latest
    steps:
      - name: 안내
        run: echo "이 워크플로우는 비활성화되었습니다. 필요 시 schedule을 다시 추가해서 재활성화하세요."
