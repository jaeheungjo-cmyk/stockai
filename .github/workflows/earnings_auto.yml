name: 기업별 실적 자동 분석 (비활성화됨)

on:
  workflow_dispatch:  # 자동 스케줄 제거 — 수동 실행만 가능 (현재 사용 안 함)

permissions:
  contents: read

jobs:
  earnings-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: 안내
        run: echo "이 워크플로우는 비활성화되었습니다. 기업별 실적은 수동 업로드로 관리합니다."
